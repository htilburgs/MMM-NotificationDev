# MMM-NotificationDev
MagicMirror module that shows Notifications (for development)
```
{
    module: "MMM-NotificationDev",
    position: "bottom_bar",
    config: {
        maxNotifications: 200,
        showPayload: true
    }
}
```
